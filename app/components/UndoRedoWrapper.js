'use client'

// Composant wrapper pour undo/redo - temporairement désactivé pour éviter les bugs
export default function UndoRedoWrapper({ children }) {
  // TODO: Réimplémenter undo/redo correctement sans interférer avec le fonctionnement normal
  return children
}

